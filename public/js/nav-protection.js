function checkUserAccess(requiredRoles) {
    // Código para verificar si el usuario tiene acceso
  }
  
  function redirectToLogin() {
    // Código para redirigir al login guardando la URL actual
  }
  
  function redirectToHome() {
    // Código para redirigir a la página principal si no tiene permisos
  }